name: Deploy web app

# Builds the Vue app in web/ and publishes web/dist to GitHub Pages on every push to master (and on
# demand). Everything is static, so Pages serves it with no server and no special headers.
on:
  push:
    branches: [master]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

defaults:
  run:
    working-directory: web

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: web/package-lock.json
      - run: npm ci
      - uses: actions/configure-pages@v5
        id: pages
      # The Pages config decides the base: a custom domain serves at the root (empty base_path),
      # while a plain fork serves under /<repo>/, so the same workflow deploys both.
      - run: npm run build -- --base=${{ steps.pages.outputs.base_path }}/
      - name: Disable Jekyll
        run: touch dist/.nojekyll
      - uses: actions/upload-pages-artifact@v3
        with:
          path: web/dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
